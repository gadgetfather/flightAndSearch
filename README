/ 
 src
  index.js/
  models/
  controllers/
  middlewares/
  services/
  utils/
  config/
  repository/ (used to write logic where orm writes/reads from DB)

  to create models
  npx sequelize model:generate

  to migrate DB
  npx sequelize db:migrate

  to undo migration
  npx sequelize db:migrate:undo